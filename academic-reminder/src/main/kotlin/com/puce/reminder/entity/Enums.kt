package com.puce.reminder.entity

enum class ReminderType { TASK, EXAM, PROJECT, PRESENTATION, OTHER }
enum class ReminderPriority { LOW, MEDIUM, HIGH, URGENT }
enum class ReminderStatus { PENDING, COMPLETED, EXPIRED }
enum class AttendanceStatus { PRESENT, ABSENT, LATE, EXCUSED }
